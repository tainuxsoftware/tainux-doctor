apiVersion: v1
kind: Service
metadata:
  name: tainux-doctor-api
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: tainux-doctor-api
  ports:
    - name: http
      port: 8080
      targetPort: http
